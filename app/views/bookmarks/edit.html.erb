<h1>Edit Comment for <%= @bookmark.movie.title %> in <%= @list.name %></h1>

<%= form_with model: [@list, @bookmark], local: true do |f| %>
  <%= f.label :comment %><br>
  <%= f.text_area :comment, rows: 3 %><br>
  <%= f.submit "Save" %>
<% end %>

<%= link_to "Back to List", list_path(@list) %>
