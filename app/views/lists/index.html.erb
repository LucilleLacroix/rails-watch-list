<h1>All Lists</h1>

<ul>
  <% @lists.each do |list| %>
    <li>
      <%= list.name %>
      <%= link_to "View Details", list_path(list), class: "btn btn-primary btn-sm" %>
    </li>
  <% end %>
</ul>

<%= link_to "Create New List", new_list_path, class: "btn btn-success" %>
