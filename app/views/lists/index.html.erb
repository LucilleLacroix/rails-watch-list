<h1>All Lists</h1>

<ul>
  <% @lists.each do |list| %>
    <li>
      <%= list.name %>
      <%= link_to "Voir plus", list_path(list), class: "btn btn-primary" %>
    </li>
  <% end %>
</ul>

<%= link_to "Créer une nouvelle liste", new_list_path, class: "btn btn-success" %>
